ServerRoot "/usr/local/apache2"

Listen 80
Listen 443

# Core Modules
LoadModule mpm_prefork_module modules/mod_mpm_prefork.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule authz_host_module modules/mod_authz_host.so

# Functionality Modules
LoadModule mime_module modules/mod_mime.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule env_module modules/mod_env.so
LoadModule headers_module modules/mod_headers.so
LoadModule setenvif_module modules/mod_setenvif.so
LoadModule dir_module modules/mod_dir.so
LoadModule alias_module modules/mod_alias.so
LoadModule rewrite_module modules/mod_rewrite.so
LoadModule socache_shmcb_module modules/mod_socache_shmcb.so
LoadModule ssl_module modules/mod_ssl.so

# PHP Module
LoadModule php_module /usr/lib/apache2/mod_php82.so

<IfModule unixd_module>
    User www-data
    Group www-data
</IfModule>

ServerAdmin admin@localhost

# Deny access to root filesystem by default
<Directory />
    AllowOverride none
    Require all denied
</Directory>

# Directory Index
<IfModule dir_module>
    DirectoryIndex index.php index.html
</IfModule>

# Protect .ht files
<Files ".ht*">
    Require all denied
</Files>

# Logging
ErrorLog "logs/error_log"
LogLevel warn

<IfModule log_config_module>
    LogFormat "{\"time\":\"%t\", \"remote_ip\":\"%a\", \"host\":\"%v\", \"request\":\"%r\", \"status\":%>s, \"user_agent\":\"%{User-Agent}i\"}" json
    CustomLog "logs/access_log" json
</IfModule>

# MIME Types
<IfModule mime_module>
    TypesConfig conf/mime.types
    AddType application/x-httpd-php .php
</IfModule>

# Virtual Hosts
Include conf/extra/vhosts/*.conf
